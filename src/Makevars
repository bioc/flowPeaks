# set by configure
GSL_CFLAGS = -I/usr/include
GSL_LIBS   = -L/usr/lib -lgsl -lgslcblas -lm

# Users customizations
# To add a customized GSL include location (<path-to-include>) where 
# the GSL header files are stored in the sub folder gsl,  and 
# the GSL library location (<path-to-lib>) where the lib files are stored. 
# change the last two lines as below:
 
# PKG_CXXFLAGS = $(GSL_CFLAGS) -I. -I<path-to-include>
# PKG_LIBS = $(GSL_LIBS) -L<path-to-lib>

PKG_CXXFLAGS =  $(GSL_CFLAGS) -I.
PKG_LIBS = $(GSL_LIBS)
